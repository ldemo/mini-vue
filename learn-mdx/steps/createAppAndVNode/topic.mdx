<CH.Section className="mb-5">
本系列主要学习以 `Vue` 为模版的前端框架技术，`逐步` 实现 `Vue` 的核心原理。
<br/>
在混沌初开的年代，`document.getElementById` 等方法是操作 `DOM` 最常见的手段。
后来 `Jquery` 横空出世，大放异彩。然，日益复杂的页面形式和交互同落后的生产力不断碰撞，
处理数据的同时兼顾页面渲染的工作模式让每一个前端工程师疲惫不堪。
`精力被分散的` 的模式应该得到改善。那如何改变？这是本系列想要说明的东西。 
（`tips`：代码会尽量对齐 `Vue` 源码，方便自行学习源码。）
<br />
下面进入正文，明确目标。  
第一步，定义 `App` 组件。    
第二步，创建 `vue` 实例，并挂载。
<CH.Code>
```vue App.vue
<template>
	<div id="demo">hello world</div>
</template>

<script>
	export default {}
</script>
```
</CH.Code>
</CH.Section>
<CH.Code className="mb-20">
```js main.js
	import App from './App.vue'

	createApp(App).mount('#root')
```
</CH.Code>
